import { PegColor } from './types';

export const COLORS = [
  PegColor.RED,
  PegColor.GREEN,
  PegColor.BLUE,
  PegColor.YELLOW,
  PegColor.PURPLE,
  PegColor.CYAN
];

export const BOARD_ROWS = 10;
export const CODE_LENGTH = 4;

export const CANVAS_WIDTH = 350; // Reduced to fit standard mobile screens
export const CANVAS_HEIGHT = 650;

// Canvas Layout
export const MARGIN_TOP = 80;
export const ROW_HEIGHT = 50;
export const PEG_RADIUS = 18;
export const PEG_SPACING = 50;
export const FEEDBACK_RADIUS = 6;
export const BOARD_LEFT_MARGIN = 40;